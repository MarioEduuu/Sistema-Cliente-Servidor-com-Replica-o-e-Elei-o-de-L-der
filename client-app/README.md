# client-app

Esta pasta foi preenchida com o projeto `entrega_chat_peer` como base reaproveitada.

Importante:
- o codigo atual ainda esta no modelo peer-to-peer
- para o trabalho final, ele deve ser adaptado para enviar requisicoes HTTP ao `gateway-service`
- o reaproveitamento aqui preserva o material que voce ja tinha, sem misturar com os servidores
