# server-replica2

Replica criada a partir de `server-replica1`.

Ajustes aplicados:
- pacote base renomeado para `com.unifor.br.server_replica2`
- classe principal `ServerReplica2Application`
- nome da aplicacao ajustado para `server-replica2`
- porta padronizada para 8083
- pasta `data/` criada para persistencia local
