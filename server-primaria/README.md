# server-primaria

Base reaproveitada do zip `server-primary`.

Ajustes aplicados nesta organizacao:
- porta padronizada para 8081
- arquivo `application.yml` corrigido
- pasta `data/` criada para persistencia local
