# server-replica1

Base reaproveitada do zip `server-replica`.

Ajustes aplicados nesta organizacao:
- nome da aplicacao ajustado para `server-replica1`
- porta padronizada para 8082
- pasta `data/` criada para persistencia local
